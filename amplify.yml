version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd amplify-app
    build:
      commands:
        - npm run start
  artifacts:

    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
